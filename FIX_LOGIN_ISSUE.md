# 🔧 Исправление проблемы входа

## 📋 Шаг 1: Проверить пользователей в базе данных

Запустите скрипт для проверки пользователей:

```bash
npm run check-users
```

Этот скрипт:
- Покажет всех пользователей в базе данных
- Проверит, активны ли они
- Проверит пароли
- Создаст тестовых пользователей, если их нет
- Активирует неактивных пользователей

## 📋 Шаг 2: Создать тестовых пользователей (если их нет)

Если пользователей нет, запустите:

```bash
npm run seed
```

Это создаст:
- **Суперадминистратор**: `admin@marina-crm.com` / `admin123`
- **Владелец клуба**: `owner@yachtclub.com` / `owner123`
- **Судовладелец**: `vessel@owner.com` / `vessel123`

## 📋 Шаг 3: Проверить консоль браузера

1. Откройте frontend в браузере
2. Нажмите **F12** (или **Ctrl+Shift+I**)
3. Перейдите на вкладку **Console**
4. Попробуйте войти
5. Найдите сообщения:
   - `API Error:` - детали ошибки API
   - `Login error:` - ошибка при входе

**Что искать:**
- `status:` - код статуса HTTP (401, 404, 500, и т.д.)
- `data:` - данные об ошибке от сервера
- `message:` - сообщение об ошибке

## 📋 Шаг 4: Проверить Network tab

1. Откройте **DevTools** (F12)
2. Перейдите на вкладку **Network**
3. Попробуйте войти
4. Найдите запрос к `/api/auth/login`
5. Проверьте:
   - **Method:** должен быть `POST`
   - **Status:** код статуса (200, 401, 404, 500, и т.д.)
   - **Request Payload:** данные запроса
   - **Response:** ответ от сервера

## 📋 Шаг 5: Проверить логи Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Найдите ваш проект `Marina-crm`
3. Перейдите в **Deployments**
4. Выберите последний деплой
5. Откройте **Logs**
6. Попробуйте войти через frontend
7. Найдите логи с `[Auth Router]` и `[DB Init]`

**Что искать:**
- `✅ База данных подключена` - БД работает
- `[Auth Router] POST /login` - запрос доходит до роутера
- `[DB Init]` - инициализация БД
- Ошибки подключения к БД
- Ошибки обработки запроса

## 🐛 Типичные проблемы и решения

### Проблема 1: "Неверный email/телефон или пароль"

**Причины:**
1. Пользователь не существует
2. Пользователь неактивен (`isActive = false`)
3. Неверный пароль

**Решение:**
1. Запустите `npm run check-users` для проверки пользователей
2. Убедитесь, что пользователь активен (`isActive = true`)
3. Проверьте правильность пароля
4. Если нужно, сбросьте пароль: `npm run reset-admin <новый_пароль>`

### Проблема 2: "База данных не подключена"

**Причины:**
1. Переменные окружения не настроены в Vercel
2. Неправильный connection string
3. Проект Supabase приостановлен

**Решение:**
1. Проверьте переменные окружения в Vercel Dashboard (см. `VERCEL_DATABASE_SETUP.md`)
2. Убедитесь, что `DATABASE_URL` установлен правильно
3. Проверьте статус проекта Supabase

### Проблема 3: "Ошибка сети" или "net::ERR_FAILED"

**Причины:**
1. Неправильный `VITE_API_URL`
2. CORS ошибка
3. Сервер недоступен

**Решение:**
1. Проверьте `VITE_API_URL` в `client/.env` (должен быть `https://marina-crm.vercel.app/api`)
2. Убедитесь, что сервер работает (проверьте `/health`)
3. Проверьте CORS настройки в Vercel

### Проблема 4: 404 "Маршрут не найден"

**Причины:**
1. Неправильный URL API
2. Маршрут не зарегистрирован

**Решение:**
1. Проверьте `VITE_API_URL` - должен заканчиваться на `/api`
2. Проверьте логи Vercel - должен быть `[Auth Router] POST /login`

### Проблема 5: 500 "Ошибка сервера"

**Причины:**
1. Ошибка в коде сервера
2. Проблема с базой данных
3. Неправильная обработка запроса

**Решение:**
1. Проверьте логи Vercel для деталей ошибки
2. Проверьте подключение к базе данных
3. Проверьте, что все переменные окружения установлены

## ✅ Учетные данные для входа (по умолчанию)

1. **Суперадминистратор:**
   - Email: `admin@marina-crm.com`
   - Пароль: `admin123`

2. **Владелец клуба:**
   - Email: `owner@yachtclub.com`
   - Пароль: `owner123`

3. **Судовладелец:**
   - Email: `vessel@owner.com`
   - Пароль: `vessel123`

## 🔍 Пошаговая отладка

1. **Проверьте пользователей:**
   ```bash
   npm run check-users
   ```

2. **Если пользователей нет, создайте их:**
   ```bash
   npm run seed
   ```

3. **Проверьте пароль суперадмина:**
   ```bash
   npm run reset-admin admin123
   ```

4. **Проверьте работу API:**
   - Откройте `https://marina-crm.vercel.app/health` - должен вернуть `{"status":"ok"}`
   - Попробуйте войти через frontend

5. **Проверьте логи:**
   - Консоль браузера (F12)
   - Логи Vercel Dashboard

## 📞 Если проблема не решена

1. Проверьте все логи (браузер, Vercel)
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что база данных подключена
4. Проверьте, что пользователи существуют и активны
5. Пришлите детали ошибки из консоли браузера и логов Vercel

