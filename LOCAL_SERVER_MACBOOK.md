# 🚀 Запуск локального сервера на MacBook

## 📋 Шаг 1: Установка зависимостей

### 1.1 Установка Node.js (если не установлен)

Проверьте, установлен ли Node.js:

```bash
node --version
npm --version
```

Если не установлен, установите через Homebrew:

```bash
brew install node
```

Или скачайте с [nodejs.org](https://nodejs.org/)

### 1.2 Установка зависимостей проекта

```bash
cd marina-crm
npm install
```

## 📋 Шаг 2: Настройка базы данных

### Вариант 1: Использование Supabase (рекомендуется)

Если вы используете Supabase, настройте переменные окружения в файле `.env`:

```env
# База данных Supabase
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres

# ИЛИ используйте отдельные переменные:
DB_HOST=aws-0-[REGION].pooler.supabase.com
DB_PORT=6543
DB_NAME=postgres
DB_USER=postgres.[PROJECT-REF]
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d

# Server
PORT=3001
NODE_ENV=development

# Frontend
FRONTEND_URL=http://localhost:5173
```

### Вариант 2: Локальная база данных PostgreSQL

Если используете локальную базу данных:

1. Установите PostgreSQL через Homebrew:

```bash
brew install postgresql@14
brew services start postgresql@14
```

2. Создайте базу данных:

```bash
createdb marina_crm
```

3. Настройте `.env`:

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=marina_crm
DB_USER=your_username
DB_PASSWORD=your_password

JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d

PORT=3001
NODE_ENV=development

FRONTEND_URL=http://localhost:5173
```

## 📋 Шаг 3: Создание файла .env

Создайте файл `.env` в корне проекта:

```bash
touch .env
```

Откройте файл `.env` и добавьте переменные окружения (см. Шаг 2).

## 📋 Шаг 4: Инициализация базы данных

### 4.1 Синхронизация схемы базы данных

```bash
npm run migrate
```

Это создаст все таблицы в базе данных.

### 4.2 Создание тестовых пользователей (опционально)

```bash
npm run seed
```

Это создаст:
- **Суперадминистратор**: `admin@marina-crm.com` / `admin123`
- **Владелец клуба**: `owner@yachtclub.com` / `owner123`
- **Судовладелец**: `vessel@owner.com` / `vessel123`

## 📋 Шаг 5: Запуск сервера

### Вариант 1: Запуск только backend сервера

```bash
npm run dev:server
```

Сервер будет доступен по адресу: `http://localhost:3001`

### Вариант 2: Запуск backend и frontend вместе

```bash
npm run dev
```

Это запустит:
- **Backend** на `http://localhost:3001`
- **Frontend** на `http://localhost:5173`

## 📋 Шаг 6: Проверка работы сервера

### 6.1 Проверка health endpoint

Откройте в браузере или выполните:

```bash
curl http://localhost:3001/health
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "timestamp": "2025-01-10T19:00:00.000Z"
}
```

### 6.2 Проверка API endpoints

```bash
# Тест входа
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "emailOrPhone": "admin@marina-crm.com",
    "password": "admin123"
  }'
```

## 📋 Шаг 7: Запуск frontend (опционально)

Если нужно запустить frontend отдельно:

```bash
cd client
npm install
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:5173`

**Важно:** Убедитесь, что файл `client/.env` содержит:

```env
VITE_API_URL=http://localhost:3001/api
```

## 🐛 Решение проблем

### Проблема: "База данных не подключена"

**Решение:**
1. Проверьте переменные окружения в `.env`
2. Убедитесь, что база данных доступна
3. Проверьте connection string для Supabase

### Проблема: "Порт 3001 занят"

**Решение:**
1. Найдите процесс, использующий порт:
```bash
lsof -i :3001
```

2. Остановите процесс:
```bash
kill -9 <PID>
```

3. Или измените порт в `.env`:
```env
PORT=3002
```

### Проблема: "Пользователи не найдены"

**Решение:**
1. Запустите seed для создания пользователей:
```bash
npm run seed
```

2. Или проверьте пользователей:
```bash
npm run check-users
```

### Проблема: "Ошибка при установке зависимостей"

**Решение:**
1. Очистите кэш npm:
```bash
npm cache clean --force
```

2. Удалите `node_modules` и `package-lock.json`:
```bash
rm -rf node_modules package-lock.json
```

3. Переустановите зависимости:
```bash
npm install
```

## 📝 Полезные команды

```bash
# Проверка пользователей
npm run check-users

# Сброс пароля суперадмина
npm run reset-admin <новый_пароль>

# Проверка подключения к базе данных
npm run check-data

# Экспорт данных
npm run export-data

# Импорт данных
npm run import-data ./exports/data_export_YYYY-MM-DDTHH-MM-SS
```

## ✅ Проверка работы

После запуска сервера проверьте:

1. ✅ Backend работает: `http://localhost:3001/health`
2. ✅ API доступен: `http://localhost:3001/api/auth/login`
3. ✅ Frontend работает: `http://localhost:5173` (если запущен)
4. ✅ База данных подключена (должна быть строка "✅ База данных подключена" в логах)

## 🎯 Следующие шаги

1. ✅ Запустите сервер: `npm run dev:server`
2. ✅ Проверьте health endpoint: `http://localhost:3001/health`
3. ✅ Попробуйте войти через frontend: `http://localhost:5173/login`
4. ✅ Используйте учетные данные: `admin@marina-crm.com` / `admin123`

---

**Важно:** Убедитесь, что все переменные окружения настроены в файле `.env` перед запуском сервера!

