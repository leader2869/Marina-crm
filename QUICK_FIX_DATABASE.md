# 🔧 Быстрое исправление: База данных не подключена

## ✅ Шаг 1: Проверьте переменные окружения в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `Marina-crm`
3. Перейдите в **Settings → Environment Variables**
4. Убедитесь, что установлена переменная `DATABASE_URL`

### Формат DATABASE_URL для Supabase:

```
postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

**Важно:** Используйте **Session Pooler** (порт 6543), а не Direct Connection!

## 🔍 Шаг 2: Проверьте логи в Vercel

1. В Vercel Dashboard перейдите в **Deployments**
2. Выберите последний деплой
3. Откройте **Logs**
4. Найдите строки с `[DB Init]`

### Что искать в логах:

✅ **Хорошо:**
```
[DB Init] DATABASE_URL: ✅ установлен
✅ База данных подключена
```

❌ **Плохо:**
```
[DB Init] DATABASE_URL: ❌ отсутствует
❌ Ошибка при подключении к базе данных
```

## 🛠️ Шаг 3: Если DATABASE_URL отсутствует

### Вариант A: Использовать DATABASE_URL (рекомендуется)

1. Получите connection string из Supabase:
   - Откройте [Supabase Dashboard](https://supabase.com/dashboard)
   - Settings → Database → Connection string
   - Выберите **Session Pooler** (порт 6543)
   - Скопируйте connection string

2. Добавьте в Vercel:
   - Settings → Environment Variables
   - Key: `DATABASE_URL`
   - Value: ваш connection string
   - Environment: Production, Preview, Development (выберите все)
   - Сохраните

### Вариант B: Использовать отдельные переменные

Если не можете использовать DATABASE_URL, добавьте:

```env
DB_HOST=aws-0-[REGION].pooler.supabase.com
DB_PORT=6543
DB_NAME=postgres
DB_USER=postgres.[PROJECT-REF]
DB_PASSWORD=your_password
```

## 🔄 Шаг 4: Передеплой

После добавления/изменения переменных окружения:

1. В Vercel Dashboard → Deployments
2. Найдите последний деплой
3. Нажмите **⋮** (три точки)
4. Выберите **Redeploy**
5. Дождитесь завершения

## ✅ Шаг 5: Проверка

1. Откройте: `https://your-app.vercel.app/health`
2. Должен вернуться:
```json
{
  "status": "ok",
  "timestamp": "..."
}
```

3. Проверьте логи снова - должно быть:
```
✅ База данных подключена
```

## 🐛 Частые проблемы

### Проблема: "DATABASE_URL отсутствует"
**Решение:** Добавьте переменную `DATABASE_URL` в Vercel Dashboard

### Проблема: "connection refused"
**Решение:** 
- Убедитесь, что используете Session Pooler (порт 6543)
- Проверьте, что проект Supabase активен (не приостановлен)

### Проблема: "password authentication failed"
**Решение:**
- Проверьте пароль в Supabase Dashboard
- Убедитесь, что connection string правильно отформатирован

### Проблема: "таймаут инициализации"
**Решение:**
- Проверьте, что проект Supabase активен
- Убедитесь, что используете правильный connection string
- Попробуйте использовать Session Pooler вместо Direct Connection

## 📞 Нужна помощь?

Если проблема не решена:
1. Скопируйте логи из Vercel Dashboard (раздел Logs)
2. Проверьте, что все переменные окружения установлены
3. Убедитесь, что проект Supabase активен

