# 💳 Реализация системы автоматических платежей

## ✅ Что реализовано

### 1. Обновлена модель Payment
- Добавлены новые поля:
  - `paymentType` - тип платежа (залог, частичный, полный, помесячный, пеня, возврат)
  - `paymentOrder` - порядок платежа (0 для залога, 1, 2, 3... для основных)
  - `paymentMonth` - номер месяца для помесячной оплаты (1-12)
  - `refundedPaymentId` - ссылка на исходный платеж при возврате
  - `externalTransactionId` - ID транзакции в платежной системе

### 2. Создан PaymentService
Сервис для автоматического создания и управления платежами:
- `createPaymentsForBooking()` - автоматически создает платежи при бронировании
- `areRequiredPaymentsPaid()` - проверяет, все ли обязательные платежи оплачены
- `getPaymentSchedule()` - возвращает график платежей для бронирования

### 3. Обновлен контроллер бронирований
- При создании бронирования автоматически создаются платежи
- Добавлен endpoint `GET /api/bookings/:id/payment-schedule` для получения графика платежей

### 4. Улучшен контроллер платежей
- При оплате платежа автоматически проверяется, нужно ли подтверждать бронирование
- Бронирование подтверждается только когда оплачены все обязательные платежи (залог + первый платеж)

## 📋 Логика создания платежей

### Сезонная оплата (SEASON_PAYMENT)

**С залогом:**
1. Залог (paymentOrder = 0) - сразу при бронировании
2. Основной платеж (paymentOrder = 1) - за 14 дней до начала сезона

**Без залога:**
1. Один платеж на всю сумму (paymentOrder = 1) - за 14 дней до начала сезона

### Помесячная оплата (MONTHLY_PAYMENT)

**С залогом:**
1. Залог (paymentOrder = 0) - сразу при бронировании
2. Платеж за первый месяц (paymentOrder = 1, paymentMonth = месяц)
3. Платеж за второй месяц (paymentOrder = 2, paymentMonth = месяц)
4. И так далее...

**Без залога:**
1. Платеж за первый месяц (paymentOrder = 1)
2. Платеж за второй месяц (paymentOrder = 2)
3. И так далее...

### Стандартное бронирование (без тарифа)

**С залогом:**
1. Залог (paymentOrder = 0) - сразу при бронировании
2. Основной платеж (paymentOrder = 1) - за 14 дней до startDate

**Без залога:**
1. Один платеж на всю сумму (paymentOrder = 1) - за 14 дней до startDate

## 🔧 Настройка залога

Залог настраивается через правила бронирования (`BookingRule` с типом `REQUIRE_DEPOSIT`):

```json
{
  "ruleType": "require_deposit",
  "parameters": {
    "depositAmount": 50000  // Фиксированная сумма
    // ИЛИ
    "depositPercentage": 30  // Процент от totalPrice
  }
}
```

## 📡 API Endpoints

### Получить график платежей
```
GET /api/bookings/:id/payment-schedule
```

**Ответ:**
```json
{
  "payments": [
    {
      "paymentId": 1,
      "type": "deposit",
      "amount": 50000,
      "dueDate": "2025-01-15",
      "status": "pending",
      "paymentOrder": 0,
      "month": null,
      "paidDate": null
    },
    {
      "paymentId": 2,
      "type": "full",
      "amount": 150000,
      "dueDate": "2025-04-17",
      "status": "pending",
      "paymentOrder": 1,
      "month": null,
      "paidDate": null
    }
  ],
  "totalAmount": 200000,
  "paidAmount": 0,
  "remainingAmount": 200000,
  "nextPaymentDue": "2025-01-15"
}
```

## 🔄 Процесс подтверждения бронирования

Бронирование автоматически переходит в статус `CONFIRMED`, когда:
1. Оплачен залог (если требуется) - `paymentOrder = 0` со статусом `PAID`
2. И оплачен первый основной платеж - `paymentOrder = 1` со статусом `PAID`

Если залог не требуется, достаточно оплатить первый платеж.

## 🗄️ Миграция базы данных

Выполните SQL миграцию для добавления новых полей:

```bash
psql -d your_database -f src/database/add-payment-fields.sql
```

Или выполните SQL вручную через клиент базы данных.

## 📝 Примеры использования

### Создание бронирования с автоматическими платежами

```typescript
POST /api/bookings
{
  "clubId": 1,
  "berthId": 5,
  "vesselId": 10,
  "tariffId": 3,  // Сезонная оплата с залогом
  "autoRenewal": false
}

// Автоматически создаются платежи:
// 1. Залог: 60000 ₽ (срок: сразу)
// 2. Основной: 140000 ₽ (срок: за 14 дней до начала)
```

### Получение графика платежей

```typescript
GET /api/bookings/123/payment-schedule

// Возвращает полный график со всеми платежами
```

### Оплата платежа

```typescript
PUT /api/payments/1/status
{
  "status": "paid",
  "paidDate": "2025-01-15",
  "transactionId": "TXN-12345"
}

// Если это залог или первый платеж, бронирование автоматически подтверждается
```

## ⚠️ Важные замечания

1. **Порядок роутов важен**: endpoint `/payment-schedule` должен быть перед `/:id` в роутах
2. **Обработка ошибок**: Если создание платежей не удалось, бронирование все равно создается (ошибка логируется)
3. **Обратная совместимость**: Старые платежи будут иметь `paymentType = null` и `paymentOrder = 0`

## 🚀 Следующие шаги

1. ✅ Базовая функциональность реализована
2. ⏳ Интеграция с платежными системами (ЮKassa, Stripe)
3. ⏳ Email/SMS уведомления о платежах
4. ⏳ Возврат средств при отмене
5. ⏳ Улучшенная система пеней
6. ⏳ Фронтенд для отображения графика платежей

