# 🚀 Настройка проекта Marina CRM на новом ПК

## Шаг 1: Установка Node.js

На macOS есть несколько способов установить Node.js:

### Вариант 1: Через официальный установщик (рекомендуется для начинающих)

1. Перейдите на сайт: https://nodejs.org/
2. Скачайте LTS версию (рекомендуется)
3. Установите скачанный файл `.pkg`
4. Проверьте установку в терминале:
```bash
node --version
npm --version
```

### Вариант 2: Через Homebrew (для продвинутых пользователей)

Если у вас установлен Homebrew:
```bash
brew install node
```

Если Homebrew не установлен, сначала установите его:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```
(потребуется ввод пароля администратора)

### Вариант 3: Через nvm (Node Version Manager)

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.zshrc
nvm install --lts
nvm use --lts
```

## Шаг 2: Установка зависимостей фронтенда

После установки Node.js выполните:

```bash
cd /Users/vitalymacbookair/Marina-crm/client
npm install
```

## Шаг 3: Настройка переменных окружения

Создайте файл `.env` в папке `client`:

```bash
cd /Users/vitalymacbookair/Marina-crm/client
cat > .env << EOF
VITE_API_URL=http://localhost:3001/api
EOF
```

## Шаг 4: Запуск фронтенда

```bash
cd /Users/vitalymacbookair/Marina-crm/client
npm run dev
```

Фронтенд будет доступен по адресу: **http://localhost:5173**

## ⚠️ Важно

Перед запуском фронтенда убедитесь, что:
- ✅ Backend сервер запущен на порту 3001
- ✅ База данных настроена и доступна
- ✅ Переменные окружения backend настроены

## 🔧 Проверка установки

После установки Node.js проверьте версии:
```bash
node --version  # Должна быть версия 18.x или выше
npm --version   # Должна быть версия 9.x или выше
```

## 📝 Следующие шаги

1. Установите Node.js (выберите один из вариантов выше)
2. Выполните: `cd client && npm install`
3. Создайте `.env` файл (см. Шаг 3)
4. Запустите: `npm run dev`
5. Откройте браузер: http://localhost:5173

---

**Готово!** После выполнения этих шагов фронтенд будет готов к работе! 🎉

