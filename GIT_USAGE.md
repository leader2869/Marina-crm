# Использование Git для отката изменений

## 📦 Текущее состояние

✅ Git репозиторий инициализирован
✅ Все файлы добавлены в репозиторий
✅ Создан первый коммит: `8af74c2`
✅ Создан тег версии: `v1.0.0`

## 🔄 Основные команды для отката

### 1. Просмотр истории коммитов

```bash
git log --oneline
```

Показывает список всех коммитов с их ID и сообщениями.

### 2. Просмотр изменений в файлах

```bash
git status
```

Показывает, какие файлы изменены, добавлены или удалены.

### 3. Откат к предыдущему коммиту (сохраняя изменения)

```bash
git reset --soft HEAD~1
```

Откатывает последний коммит, но сохраняет все изменения в рабочей директории.

### 4. Откат к предыдущему коммиту (удаляя изменения)

```bash
git reset --hard HEAD~1
```

⚠️ **ВНИМАНИЕ:** Эта команда удалит все изменения! Используйте осторожно.

### 5. Откат к конкретному коммиту

```bash
git reset --hard <commit-id>
```

Например:
```bash
git reset --hard 8af74c2
```

### 6. Откат к тегу версии

```bash
git reset --hard v1.0.0
```

Откатывает проект к версии 1.0.0.

### 7. Откат конкретного файла

```bash
git checkout -- <путь-к-файлу>
```

Например:
```bash
git checkout -- src/modules/users/users.controller.ts
```

### 8. Просмотр изменений в файле

```bash
git diff <путь-к-файлу>
```

Показывает, что изменилось в файле.

## 📝 Создание новых коммитов

### 1. Добавить изменения

```bash
git add .
```

Или конкретный файл:
```bash
git add src/modules/users/users.controller.ts
```

### 2. Создать коммит

```bash
git commit -m "Описание изменений"
```

### 3. Создать тег для важной версии

```bash
git tag -a v1.1.0 -m "Описание версии"
```

## 🔍 Полезные команды

### Просмотр всех тегов

```bash
git tag -l
```

### Просмотр информации о коммите

```bash
git show <commit-id>
```

### Просмотр изменений между коммитами

```bash
git diff <commit1> <commit2>
```

## ⚠️ Важные замечания

1. **`.env` файл не сохраняется в Git** - он в `.gitignore` для безопасности
2. **`node_modules` не сохраняется** - устанавливается через `npm install`
3. **Всегда проверяйте изменения** перед откатом: `git status` и `git diff`

## 📋 Примеры использования

### Сценарий 1: Откатить последние изменения

```bash
# 1. Посмотреть, что изменилось
git status

# 2. Откатить все изменения
git reset --hard HEAD

# Или откатить к конкретному коммиту
git reset --hard 8af74c2
```

### Сценарий 2: Сохранить текущее состояние перед изменениями

```bash
# 1. Создать коммит с текущим состоянием
git add .
git commit -m "Сохранение перед экспериментом"

# 2. Делать изменения...

# 3. Если нужно откатиться
git reset --hard HEAD~1
```

### Сценарий 3: Откатить только один файл

```bash
# Откатить конкретный файл к последнему коммиту
git checkout -- src/modules/users/users.controller.ts
```

## 🎯 Быстрый откат к версии 1.0.0

Если нужно вернуться к текущему сохраненному состоянию:

```bash
git reset --hard v1.0.0
```

Это вернет проект к состоянию, когда был создан первый коммит.

