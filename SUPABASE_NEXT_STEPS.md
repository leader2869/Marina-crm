# 🚀 Следующие шаги после миграции на Supabase

## ✅ Шаг 1: Схема базы данных создана

Схема базы данных успешно создана в Supabase через TypeORM synchronize. Все таблицы, связи и индексы созданы автоматически.

---

## 📝 Шаг 2: Загрузка тестовых данных (Seed)

Загрузите тестовые данные в базу данных:

```bash
npm run seed
```

Это создаст:
- ✅ Суперадминистратор: `admin@marina-crm.com` / `admin123`
- ✅ Владелец клуба: `owner@yachtclub.com` / `owner123`
- ✅ Судовладелец: `vessel@owner.com` / `vessel123`
- ✅ Тестовый яхт-клуб с 50 местами
- ✅ Категории расходов

---

## 📝 Шаг 3: Проверка подключения

Проверьте, что подключение работает корректно:

```bash
npm run test-supabase
```

Вы должны увидеть:
- ✅ Подключение к базе данных успешно
- ✅ Информация о сервере
- ✅ Список созданных таблиц

---

## 📝 Шаг 4: Проверка данных

Проверьте, что данные загружены:

```bash
# Проверка суперадминистратора
npm run check-admin

# Проверка клубов
npm run check-clubs

# Проверка валидации пользователей
npm run check-isvalidated
```

---

## 📝 Шаг 5: Запуск приложения

Запустите приложение и проверьте, что все работает:

```bash
# Запуск backend и frontend одновременно
npm run dev

# Или отдельно
npm run dev:server  # Backend на порту 3001
npm run dev:client  # Frontend на порту 5173
```

---

## 📝 Шаг 6: Вход в систему

1. Откройте браузер: `http://localhost:5173`
2. Войдите как суперадминистратор:
   - Email: `admin@marina-crm.com`
   - Пароль: `admin123`

---

## 📝 Шаг 7: Перенос данных из локальной БД (опционально)

Если у вас есть данные в локальной базе данных, которые нужно перенести:

### 7.1 Создание резервной копии локальной БД

```bash
npm run backup
```

Или вручную:

```bash
pg_dump -h localhost -U postgres -d marina_crm -Fc -f local_backup.dump
```

### 7.2 Импорт данных в Supabase

**Вариант 1: Через pg_restore (рекомендуется)**

```bash
# Используйте connection string для Session Pooler
pg_restore "postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres" --data-only --no-owner --no-acl local_backup.dump
```

**Вариант 2: Через Supabase SQL Editor**

1. Откройте Supabase Dashboard → **SQL Editor**
2. Экспортируйте данные из локальной БД в SQL формат:

```bash
pg_dump -h localhost -U postgres -d marina_crm --data-only --inserts -f data.sql
```

3. Откройте файл `data.sql` и скопируйте SQL команды
4. Вставьте в Supabase SQL Editor и выполните

---

## 📝 Шаг 8: Применение дополнительных SQL скриптов (если нужно)

Если TypeORM synchronize не создал все необходимые поля, примените SQL скрипты:

1. Откройте Supabase Dashboard → **SQL Editor**
2. Выполните скрипты по очереди (если они еще не применены):

   - `add-isvalidated-field.sql` - для поля isValidated в users
   - `add-club-isvalidated-field.sql` - для поля isValidated в clubs
   - `add-club-issubmittedforvalidation-field.sql` - для поля isSubmittedForValidation
   - `add-club-rejectioncomment-field.sql` - для поля rejectionComment
   - `add-tariffs-tables.sql` - для таблиц тарифов (если еще не созданы)

**Примечание:** TypeORM synchronize уже создал все таблицы на основе entities, поэтому большинство полей уже должны быть созданы. Проверьте через SQL Editor, какие поля уже существуют.

---

## 📝 Шаг 9: Настройка переменных окружения для production

Если вы планируете деплой приложения:

1. Обновите `.env` файл для production:
   ```env
   NODE_ENV=production
   DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
   FRONTEND_URL=https://your-domain.com
   JWT_SECRET=your-super-secret-jwt-key-change-in-production
   ```

2. Убедитесь, что SSL настроен (уже настроен автоматически для Supabase)

---

## ✅ Чек-лист после миграции

- [ ] Схема базы данных создана (`npm run migrate`)
- [ ] Тестовые данные загружены (`npm run seed`)
- [ ] Подключение проверено (`npm run test-supabase`)
- [ ] Суперадминистратор создан (`npm run check-admin`)
- [ ] Приложение запущено (`npm run dev`)
- [ ] Вход в систему работает
- [ ] Данные из локальной БД перенесены (если нужно)
- [ ] Дополнительные SQL скрипты применены (если нужно)

---

## 🎉 Готово!

Ваш проект успешно мигрирован на Supabase! Теперь вы можете:

- ✅ Использовать управляемую PostgreSQL базу данных
- ✅ Пользоваться автоматическими резервными копиями
- ✅ Масштабировать приложение
- ✅ Использовать Supabase Dashboard для управления БД

---

## 📚 Полезные ссылки

- [Supabase Dashboard](https://supabase.com/dashboard)
- [Supabase SQL Editor](https://supabase.com/dashboard/project/_/sql)
- [Supabase Documentation](https://supabase.com/docs)

---

**Удачи с проектом! 🚀**

