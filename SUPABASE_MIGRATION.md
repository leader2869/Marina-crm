# 🚀 Миграция проекта на Supabase

## 📋 Обзор

Это руководство поможет вам перенести весь проект Marina CRM и базу данных на Supabase. Supabase предоставляет управляемый PostgreSQL с дополнительными возможностями.

## ✅ Преимущества Supabase

- ✅ Управляемый PostgreSQL (не нужно устанавливать локально)
- ✅ Автоматические резервные копии
- ✅ Масштабируемость
- ✅ Безопасность и SSL по умолчанию
- ✅ Веб-интерфейс для управления БД
- ✅ REST API из коробки
- ✅ Realtime подписки (опционально)

---

## 📝 Шаг 1: Создание проекта в Supabase

### 1.1 Регистрация

1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите **"Start your project"** или **"Sign up"**
3. Войдите через GitHub, Google или создайте аккаунт

### 1.2 Создание проекта

1. Нажмите **"New Project"**
2. Заполните данные:
   - **Name**: `marina-crm` (или любое другое имя)
   - **Database Password**: создайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион
   - **Pricing Plan**: выберите подходящий план (Free tier доступен)

3. Нажмите **"Create new project"**
4. Дождитесь создания проекта (обычно 1-2 минуты)

---

## 📝 Шаг 2: Получение данных подключения

### 2.1 Connection String

1. В проекте Supabase перейдите в **Settings** → **Database**
2. Найдите раздел **"Connection string"**
3. **ВАЖНО**: Если вы видите сообщение "Not IPv4 compatible", используйте **Session Pooler** (см. ниже)
4. Для миграций и админ-задач используйте **Session Pooler** (порт 6543)
5. Выберите вкладку **"URI"** в разделе **"Session Pooler"**
6. Скопируйте connection string (он будет выглядеть так):
   ```
   postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
   ```

**Примечание:**
- Direct connection (порт 5432) может быть несовместим с IPv4 сетями
- Session Pooler (порт 6543) работает через IPv4 и рекомендуется для большинства случаев
- Transaction Pooler (порт 6543) также работает, но Session Pooler предпочтительнее для миграций

### 2.2 Альтернативный способ (отдельные параметры)

Если хотите использовать отдельные параметры:

1. В разделе **Settings** → **Database** найдите:
   - **Host**: `db.xxxxx.supabase.co`
   - **Database name**: `postgres`
   - **Port**: `5432`
   - **User**: `postgres`
   - **Password**: пароль, который вы создали при создании проекта

---

## 📝 Шаг 3: Создание резервной копии текущей БД

⚠️ **ВАЖНО**: Перед миграцией создайте резервную копию!

```bash
# Создайте резервную копию текущей базы данных
npm run backup

# Или вручную через pg_dump
pg_dump -h localhost -U postgres -d marina_crm -Fc -f backup_before_supabase.dump
```

---

## 📝 Шаг 4: Обновление конфигурации проекта

### 4.1 Обновление database.ts

Файл `src/config/database.ts` уже поддерживает connection string. Если используете connection string, обновите его:

```typescript
export const AppDataSource = new DataSource({
  type: 'postgres',
  // Вариант 1: Использование connection string
  url: process.env.DATABASE_URL, // Если используете connection string
  
  // Вариант 2: Отдельные параметры (текущий вариант)
  host: process.env.DB_HOST || 'localhost',
  port: parseInt(process.env.DB_PORT || '5432'),
  username: process.env.DB_USER || 'postgres',
  password: process.env.DB_PASSWORD || 'postgres',
  database: process.env.DB_NAME || 'marina_crm',
  
  // ... остальные настройки
});
```

### 4.2 Обновление .env файла

Создайте или обновите файл `.env` в корне проекта:

**Вариант 1: Использование connection string (рекомендуется)**

**Для IPv4 сетей используйте Session Pooler:**

```env
# Supabase Connection String (Session Pooler для IPv4)
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

**Пример:**
```env
DATABASE_URL=postgresql://postgres.toimcbqcpzusbrbqwxqn:MyPassword123@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```

# Или используйте отдельные параметры (Session Pooler для IPv4):
DB_HOST=aws-0-[REGION].pooler.supabase.com
DB_PORT=6543
DB_NAME=postgres
DB_USER=postgres.[PROJECT-REF]
DB_PASSWORD=ваш_пароль_от_supabase
```

**Пример:**
```env
DB_HOST=aws-0-eu-central-1.pooler.supabase.com
DB_PORT=6543
DB_NAME=postgres
DB_USER=postgres.toimcbqcpzusbrbqwxqn
DB_PASSWORD=MyPassword123

# Server Configuration
PORT=3001
NODE_ENV=production

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Frontend URL (обновите на ваш домен)
FRONTEND_URL=https://your-domain.com

# File Upload
UPLOAD_MAX_SIZE=10485760
UPLOAD_PATH=./uploads
```

⚠️ **ВАЖНО**: 
- Замените `[YOUR-PASSWORD]` на реальный пароль от Supabase
- Замените `db.xxxxx.supabase.co` на ваш реальный хост
- В production используйте переменные окружения, не храните пароли в коде!

---

## 📝 Шаг 5: Перенос схемы базы данных

### 5.1 Способ 1: Через TypeORM migrations (рекомендуется)

1. Убедитесь, что подключение к Supabase работает
2. Запустите миграции:

```bash
npm run migrate
```

### 5.2 Способ 2: Через SQL скрипты

1. Экспортируйте схему из текущей БД:

```bash
# Экспорт только схемы (без данных)
pg_dump -h localhost -U postgres -d marina_crm --schema-only -f schema.sql
```

2. Импортируйте схему в Supabase:

```bash
# Замените connection string на ваш
psql "postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres" -f schema.sql
```

### 5.3 Способ 3: Через Supabase SQL Editor

1. В проекте Supabase перейдите в **SQL Editor**
2. Скопируйте SQL скрипты из ваших миграций
3. Выполните их в SQL Editor

---

## 📝 Шаг 6: Перенос данных

### 6.1 Экспорт данных из текущей БД

```bash
# Экспорт только данных (без схемы)
pg_dump -h localhost -U postgres -d marina_crm --data-only -Fc -f data.dump
```

### 6.2 Импорт данных в Supabase

```bash
# Импорт данных
pg_restore -h db.xxxxx.supabase.co -U postgres -d postgres --data-only --no-owner --no-acl data.dump
```

Или через connection string:

```bash
pg_restore "postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres" --data-only --no-owner --no-acl data.dump
```

### 6.3 Альтернативный способ: Полный дамп

```bash
# Полный дамп (схема + данные)
pg_dump -h localhost -U postgres -d marina_crm -Fc -f full_backup.dump

# Импорт в Supabase
pg_restore "postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres" --no-owner --no-acl full_backup.dump
```

---

## 📝 Шаг 7: Применение дополнительных миграций

Если у вас есть дополнительные SQL скрипты (например, для добавления полей):

1. Откройте **SQL Editor** в Supabase
2. Выполните скрипты по очереди:
   - `add-isvalidated-field.sql`
   - `add-club-isvalidated-field.sql`
   - `add-club-issubmittedforvalidation-field.sql`
   - `add-club-rejectioncomment-field.sql`
   - `add-tariffs-tables.sql`

---

## 📝 Шаг 8: Тестирование подключения

### 8.1 Проверка подключения

Создайте тестовый скрипт `test-supabase-connection.js`:

```javascript
require('dotenv').config();
const { Client } = require('pg');

const client = new Client({
  connectionString: process.env.DATABASE_URL || 
    `postgresql://${process.env.DB_USER}:${process.env.DB_PASSWORD}@${process.env.DB_HOST}:${process.env.DB_PORT}/${process.env.DB_NAME}`
});

client.connect()
  .then(() => {
    console.log('✅ Подключение к Supabase успешно!');
    return client.query('SELECT NOW()');
  })
  .then((result) => {
    console.log('✅ Время сервера:', result.rows[0].now);
    client.end();
  })
  .catch((err) => {
    console.error('❌ Ошибка подключения:', err.message);
    process.exit(1);
  });
```

Запустите:

```bash
node test-supabase-connection.js
```

### 8.2 Проверка через приложение

```bash
# Запустите сервер
npm run dev:server

# Проверьте, что нет ошибок подключения
```

---

## 📝 Шаг 9: Настройка SSL (если требуется)

Supabase требует SSL подключение. TypeORM обычно обрабатывает это автоматически, но если возникнут проблемы, добавьте в `database.ts`:

```typescript
export const AppDataSource = new DataSource({
  // ... другие параметры
  ssl: process.env.NODE_ENV === 'production' ? {
    rejectUnauthorized: false
  } : false,
});
```

---

## 📝 Шаг 10: Обновление резервного копирования

Обновите скрипт резервного копирования для работы с Supabase:

```bash
# Создание резервной копии из Supabase
npm run backup
```

Скрипт автоматически использует параметры из `.env` файла.

---

## 🔒 Безопасность

### Переменные окружения

⚠️ **НИКОГДА** не коммитьте `.env` файл в Git!

Убедитесь, что `.env` в `.gitignore`:

```gitignore
.env
.env.local
.env.production
```

### Supabase Dashboard

- Используйте сильные пароли
- Включите 2FA для аккаунта Supabase
- Регулярно обновляйте пароли доступа к БД

---

## 🚀 Деплой приложения

### Вариант 1: Vercel / Netlify (Frontend)

1. Подключите репозиторий
2. Установите переменные окружения:
   - `VITE_API_URL` (URL вашего backend API)

### Вариант 2: Railway / Render (Backend)

1. Подключите репозиторий
2. Установите переменные окружения:
   - `DATABASE_URL` (connection string от Supabase)
   - `JWT_SECRET`
   - `FRONTEND_URL`
   - И другие необходимые переменные

---

## 📊 Мониторинг и управление

### Supabase Dashboard

- **Table Editor**: просмотр и редактирование данных
- **SQL Editor**: выполнение SQL запросов
- **Database**: настройки БД, connection pooling
- **Logs**: логи запросов и ошибок
- **Backups**: автоматические резервные копии

### Connection Pooling

Supabase предоставляет два типа подключений:

1. **Direct connection** (для миграций и админ-задач)
2. **Connection pooler** (для приложения)

Для production используйте connection pooler:

```
postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:6543/postgres
```

Обратите внимание на порт `6543` вместо `5432`.

---

## 🐛 Устранение неполадок

### Ошибка: "connection refused"

- Проверьте правильность хоста и порта
- Убедитесь, что проект Supabase активен
- Проверьте firewall настройки

### Ошибка: "password authentication failed"

- Проверьте пароль в `.env`
- Убедитесь, что используете правильный пароль от Supabase проекта

### Ошибка: "database does not exist"

- В Supabase используется база `postgres` по умолчанию
- Или создайте новую БД через SQL Editor

### Ошибка SSL

- Добавьте `ssl: { rejectUnauthorized: false }` в конфигурацию TypeORM
- Или используйте connection pooler (порт 6543)

---

## ✅ Чек-лист миграции

- [ ] Создан проект в Supabase
- [ ] Получен connection string
- [ ] Создана резервная копия текущей БД
- [ ] Обновлен `.env` файл
- [ ] Перенесена схема БД
- [ ] Перенесены данные
- [ ] Применены дополнительные миграции
- [ ] Протестировано подключение
- [ ] Протестировано приложение
- [ ] Обновлены скрипты резервного копирования
- [ ] Настроен деплой (если требуется)

---

## 📚 Дополнительные ресурсы

- [Документация Supabase](https://supabase.com/docs)
- [TypeORM документация](https://typeorm.io/)
- [PostgreSQL документация](https://www.postgresql.org/docs/)

---

## 💡 Советы

1. **Тестирование**: Сначала протестируйте на staging окружении
2. **Резервные копии**: Регулярно создавайте резервные копии
3. **Мониторинг**: Используйте Supabase Dashboard для мониторинга
4. **Производительность**: Используйте connection pooler для production
5. **Безопасность**: Храните пароли в переменных окружения, не в коде

---

**Удачи с миграцией! 🚀**

