# 🔧 Исправление ошибки подключения к Supabase

## ❌ Ошибка: `ENOTFOUND db.xxxxx.supabase.co`

Эта ошибка означает, что DNS не может найти указанный хост. Возможные причины:

1. **Проект Supabase приостановлен или удален**
2. **Неправильный хост в .env файле** (используется пример вместо реального)
3. **Проект еще не полностью создан** (нужно подождать 2-3 минуты)
4. **Проблемы с интернет-соединением**

### 🔍 Быстрая диагностика

Запустите скрипт диагностики:

```bash
npm run diagnose-supabase
```

Этот скрипт проверит:
- ✅ Наличие переменных окружения
- ✅ DNS резолюцию хоста
- ✅ Доступность порта
- ✅ Подключение к серверу

---

## ✅ Решение по шагам

### ШАГ 1: Откройте ваш проект Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите в свой аккаунт
3. Выберите ваш проект `marina-crm` (или как вы его назвали)

### ШАГ 2: Получите данные подключения

1. В левом меню нажмите **Settings** (⚙️ Настройки)
2. Выберите **Database**
3. Прокрутите вниз до раздела **"Connection string"**

### ШАГ 3: Скопируйте Connection String

Вы увидите несколько вариантов. Выберите вкладку **"URI"**:

```
postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

Или для прямого подключения (для миграций):

```
postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
```

**Важно:**
- `[PROJECT-REF]` - это уникальный идентификатор вашего проекта (например: `abcdefghijklmnop`)
- `[YOUR-PASSWORD]` - это пароль, который вы создали при создании проекта
- Для миграций используйте **direct connection** (порт 5432)
- Для приложения используйте **connection pooler** (порт 6543)

### ШАГ 4: Обновите файл .env

Откройте файл `.env` в корне проекта и замените пример на реальные данные.

**Вариант 1: Использование Connection String (рекомендуется)**

```env
# Замените на ваш реальный connection string из Supabase
DATABASE_URL=postgresql://postgres:[ВАШ_ПАРОЛЬ]@db.[ВАШ_PROJECT_REF].supabase.co:5432/postgres

# Остальные настройки
PORT=3001
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d
FRONTEND_URL=http://localhost:5173
```

**Вариант 2: Использование отдельных параметров**

```env
# Замените на ваши реальные данные из Supabase
DB_HOST=db.[ВАШ_PROJECT_REF].supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=[ВАШ_ПАРОЛЬ]

# Остальные настройки
PORT=3001
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d
FRONTEND_URL=http://localhost:5173
```

### ШАГ 5: Пример правильного .env

**Пример с реальными данными:**

```env
# Supabase Connection (замените на ваши данные!)
DATABASE_URL=postgresql://postgres:MySecurePassword123@db.abcdefghijklmnop.supabase.co:5432/postgres

# Или отдельные параметры:
# DB_HOST=db.abcdefghijklmnop.supabase.co
# DB_PORT=5432
# DB_NAME=postgres
# DB_USER=postgres
# DB_PASSWORD=MySecurePassword123

# Server
PORT=3001
NODE_ENV=development

# JWT
JWT_SECRET=my-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Frontend
FRONTEND_URL=http://localhost:5173
```

### ШАГ 6: Проверьте подключение

После обновления `.env` файла, проверьте подключение:

```bash
npm run test-supabase
```

Если подключение успешно, вы увидите:
```
✅ Подключение к базе данных успешно!
📊 Информация о сервере:
   Текущее время: ...
   PostgreSQL версия: ...
```

### ШАГ 7: Запустите миграцию

После успешной проверки подключения:

```bash
npm run migrate
```

---

## 🔍 Как найти PROJECT-REF

PROJECT-REF - это уникальный идентификатор вашего проекта. Его можно найти:

1. **В URL проекта Supabase:**
   ```
   https://supabase.com/dashboard/project/[PROJECT-REF]
   ```

2. **В connection string:**
   ```
   db.[PROJECT-REF].supabase.co
   ```

3. **В Settings → API:**
   - Project URL содержит PROJECT-REF

---

## ⚠️ Важные замечания

1. **НЕ используйте примеры** типа `db.xxxxx.supabase.co` - это только шаблоны!

2. **Пароль чувствителен к регистру** - вводите точно так, как создали

3. **Для миграций используйте direct connection** (порт 5432)

4. **Для приложения используйте connection pooler** (порт 6543) для лучшей производительности

5. **Храните пароли в безопасности** - не коммитьте `.env` в Git!

---

## 🔍 Диагностика проблемы

### Шаг 1: Проверьте статус проекта в Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Найдите ваш проект
3. Проверьте статус:
   - ✅ **Active** - проект активен
   - ⏸️ **Paused** - проект приостановлен (нужно возобновить)
   - ❌ **Deleted** - проект удален (нужно создать новый)

### Шаг 2: Запустите диагностику

```bash
npm run diagnose-supabase
```

Скрипт покажет:
- Какие переменные окружения установлены
- Работает ли DNS резолюция
- Доступен ли хост
- Доступен ли порт

### Шаг 3: Проверьте правильность хоста

Хост должен выглядеть примерно так:
```
db.abcdefghijklmnop.supabase.co
```

Где `abcdefghijklmnop` - это уникальный PROJECT-REF вашего проекта.

**НЕ используйте:**
- ❌ `db.xxxxx.supabase.co` (это пример)
- ❌ `localhost` (для Supabase)
- ❌ Любые другие примеры

### Шаг 4: Если проект только что создан

Если вы только что создали проект в Supabase:
1. Подождите 2-3 минуты после создания
2. Убедитесь, что проект полностью инициализирован
3. Проверьте в Dashboard, что статус "Active"

### Шаг 5: Попробуйте connection pooler

Если direct connection не работает, попробуйте использовать connection pooler:

**В .env файле:**
```env
# Вместо порта 5432 используйте 6543
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

Или:
```env
DB_HOST=aws-0-[REGION].pooler.supabase.com
DB_PORT=6543
```

## 🐛 Другие возможные ошибки

### Ошибка: "password authentication failed"

**Решение:**
- Проверьте правильность пароля
- Убедитесь, что используете пароль от Supabase проекта, а не от аккаунта

### Ошибка: "database does not exist"

**Решение:**
- В Supabase используется база `postgres` по умолчанию
- Убедитесь, что `DB_NAME=postgres` в `.env`

### Ошибка: "connection refused"

**Решение:**
- Проверьте, что проект Supabase активен (не приостановлен)
- Проверьте правильность порта
- Проверьте интернет-соединение

---

## ✅ Чек-лист

- [ ] Открыл проект в Supabase Dashboard
- [ ] Перешел в Settings → Database
- [ ] Скопировал connection string или отдельные параметры
- [ ] Обновил `.env` файл с реальными данными
- [ ] Заменил `db.xxxxx.supabase.co` на реальный хост
- [ ] Заменил `[YOUR-PASSWORD]` на реальный пароль
- [ ] Проверил подключение: `npm run test-supabase`
- [ ] Запустил миграцию: `npm run migrate`

---

## 📞 Нужна помощь?

Если проблема не решена:

1. Проверьте, что проект Supabase активен (не приостановлен)
2. Убедитесь, что используете правильный пароль
3. Попробуйте сгенерировать новый пароль в Supabase (Settings → Database → Reset database password)
4. Проверьте, что `.env` файл находится в корне проекта
5. Убедитесь, что нет лишних пробелов в connection string

---

**После исправления `.env` файла миграция должна пройти успешно! 🚀**

