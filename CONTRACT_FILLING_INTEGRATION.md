# 📄 Интеграция модуля "Заполнение договоров"

## ✅ Что сделано:

1. ✅ Добавлен пункт меню "Заполнение договоров" для роли `CLUB_OWNER`
2. ✅ Создана страница `ContractFilling.tsx` с React интерфейсом
3. ✅ Добавлен маршрут `/contract-filling` в `App.tsx`
4. ✅ Страница интегрирована с Flask приложением через API

## 📋 Структура:

- **Frontend**: `client/src/pages/ContractFilling.tsx` - React компонент
- **Backend**: Flask приложение из папки `/Users/vitalymacbookair/заполнение договоров`
- **API**: Flask приложение предоставляет REST API на порту 5001

## 🚀 Настройка и запуск:

### Шаг 1: Скопировать проект заполнения договоров

```bash
# Скопируйте проект в папку проекта
cp -r "/Users/vitalymacbookair/заполнение договоров" ./contract-filling-backend
```

### Шаг 2: Установить зависимости Python

```bash
cd contract-filling-backend
python3 -m pip install -r requirements.txt
```

### Шаг 3: Настроить переменные окружения

Создайте файл `.env` в корне проекта или добавьте в `client/.env`:

```env
VITE_FLASK_URL=http://localhost:5001
```

### Шаг 4: Запустить Flask приложение

```bash
cd contract-filling-backend
python3 app.py
```

Flask приложение будет доступно на `http://localhost:5001`

### Шаг 5: Запустить React приложение

```bash
cd client
npm run dev
```

React приложение будет доступно на `http://localhost:5173`

## 🔧 Интеграция с существующим backend (опционально)

Если вы хотите интегрировать функционал напрямую в Node.js backend:

1. **Создать API endpoints в Express:**
   - `/api/contract-filling/upload` - загрузка документа
   - `/api/contract-filling/templates` - список шаблонов
   - `/api/contract-filling/fill` - заполнение договора
   - `/api/contract-filling/download/:filename` - скачивание файла

2. **Использовать библиотеки для работы с DOCX:**
   - `docx` - для Node.js
   - Или вызывать Python скрипты через `child_process`

## 📝 Использование:

1. Войдите в систему как `CLUB_OWNER`
2. Перейдите в меню "Заполнение договоров"
3. Загрузите шаблон договора (.doc или .docx)
4. Система автоматически найдет якоря в формате `{{имя_якоря}}`
5. Сохраните шаблон
6. Выберите шаблон и заполните данные
7. Скачайте заполненный договор

## 🎯 Функционал:

- ✅ Загрузка документов DOC/DOCX
- ✅ Автоматическое обнаружение якорей
- ✅ Сохранение шаблонов
- ✅ Заполнение договоров данными
- ✅ Сохранение контрагентов
- ✅ Скачивание заполненных договоров

## 🔐 Доступ:

Модуль доступен только для роли `CLUB_OWNER` (Владелец яхт-клуба).

## 📂 Структура файлов:

```
Marina-crm/
├── client/
│   └── src/
│       └── pages/
│           └── ContractFilling.tsx  # React компонент
├── contract-filling-backend/         # Flask приложение (скопировать)
│   ├── app.py
│   ├── templates/
│   ├── uploads/
│   ├── doc_templates/
│   └── output/
└── ...
```

## ⚠️ Важно:

- Flask приложение должно быть запущено на порту 5001
- Убедитесь, что порт 5001 не занят другим приложением
- Для production нужно настроить проксирование запросов через Vercel или другой сервер

## 🔄 Альтернативный вариант (без Flask):

Если не хотите запускать отдельное Flask приложение, можно:

1. Переписать функционал на Node.js/TypeScript
2. Использовать библиотеку `docx` для Node.js
3. Интегрировать в основной Express backend

