# 📦 Подробное руководство: npm install

## 🎯 Что такое npm install?

`npm install` (или сокращенно `npm i`) — это команда Node Package Manager (npm), которая устанавливает все зависимости проекта, указанные в файле `package.json`.

## 🔍 Что происходит при выполнении команды?

### 1. Чтение package.json

npm читает файл `package.json` в корне проекта и анализирует:
- **dependencies** — зависимости, необходимые для работы приложения
- **devDependencies** — зависимости, нужные только для разработки

### 2. Создание node_modules

npm создает папку `node_modules/` (если её нет), куда будут установлены все пакеты.

### 3. Установка зависимостей

npm скачивает пакеты из реестра npm (npmjs.com) и устанавливает их в `node_modules/`.

### 4. Создание package-lock.json

После установки создается файл `package-lock.json`, который фиксирует точные версии всех установленных пакетов и их зависимостей.

## 📋 Зависимости проекта Marina CRM

### 🔧 Production Dependencies (dependencies)

Эти пакеты нужны для работы приложения в продакшене:

#### **express** (^4.18.2)
- **Что это:** Веб-фреймворк для Node.js
- **Зачем:** Создание REST API сервера
- **Использование:** Обработка HTTP запросов, маршрутизация

#### **cors** (^2.8.5)
- **Что это:** Middleware для Cross-Origin Resource Sharing
- **Зачем:** Разрешение запросов с других доменов (для фронтенда)
- **Использование:** Настройка CORS политики

#### **dotenv** (^16.3.1)
- **Что это:** Загрузка переменных окружения из .env файла
- **Зачем:** Хранение секретов и конфигурации
- **Использование:** Чтение DB_HOST, JWT_SECRET и т.д.

#### **bcryptjs** (^2.4.3)
- **Что это:** Библиотека для хеширования паролей
- **Зачем:** Безопасное хранение паролей в БД
- **Использование:** Хеширование при регистрации, проверка при входе

#### **jsonwebtoken** (^9.0.2)
- **Что это:** Создание и проверка JWT токенов
- **Зачем:** Аутентификация пользователей
- **Использование:** Генерация токенов при входе, проверка в middleware

#### **pg** (^8.11.3)
- **Что это:** Драйвер PostgreSQL для Node.js
- **Зачем:** Подключение к базе данных PostgreSQL
- **Использование:** TypeORM использует его для работы с БД

#### **typeorm** (^0.3.17)
- **Что это:** Object-Relational Mapping (ORM) для TypeScript/JavaScript
- **Зачем:** Работа с базой данных через объекты вместо SQL
- **Использование:** Определение сущностей, миграции, запросы

#### **class-validator** (^0.14.0)
- **Что это:** Валидация данных через декораторы
- **Зачем:** Проверка входных данных API
- **Использование:** Валидация email, паролей, обязательных полей

#### **class-transformer** (^0.5.1)
- **Что это:** Преобразование объектов
- **Зачем:** Преобразование plain объектов в классы и наоборот
- **Использование:** Работает вместе с class-validator

#### **reflect-metadata** (^0.1.13)
- **Что это:** Полифилл для метаданных TypeScript
- **Зачем:** Поддержка декораторов в TypeScript
- **Использование:** Необходим для TypeORM и class-validator

#### **multer** (^1.4.5-lts.1)
- **Что это:** Middleware для загрузки файлов
- **Зачем:** Загрузка документов, фото, файлов
- **Использование:** Загрузка документов судов, фото чеков

#### **nodemailer** (^6.9.7)
- **Что это:** Библиотека для отправки email
- **Зачем:** Отправка уведомлений, восстановление пароля
- **Использование:** Email уведомления о бронированиях, платежах

#### **uuid** (^9.0.1)
- **Что это:** Генерация уникальных идентификаторов
- **Зачем:** Создание UUID для различных целей
- **Использование:** Уникальные ID для файлов, токенов

#### **date-fns** (^2.30.0)
- **Что это:** Утилиты для работы с датами
- **Зачем:** Форматирование, вычисление разницы дат
- **Использование:** Расчет стоимости бронирования, проверка сроков

#### **express-rate-limit** (^7.1.5)
- **Что это:** Ограничение частоты запросов
- **Зачем:** Защита от DDoS, ограничение API запросов
- **Использование:** Ограничение попыток входа, API запросов

### 🛠️ Development Dependencies (devDependencies)

Эти пакеты нужны только во время разработки:

#### **@types/*** пакеты
- **Что это:** TypeScript определения типов
- **Зачем:** Поддержка TypeScript для JavaScript библиотек
- **Пакеты:**
  - `@types/express` — типы для Express
  - `@types/node` — типы для Node.js API
  - `@types/bcryptjs` — типы для bcryptjs
  - `@types/jsonwebtoken` — типы для JWT
  - `@types/cors` — типы для CORS
  - `@types/multer` — типы для Multer
  - `@types/nodemailer` — типы для Nodemailer
  - `@types/uuid` — типы для UUID

#### **typescript** (^5.3.3)
- **Что это:** Компилятор TypeScript
- **Зачем:** Компиляция TypeScript в JavaScript
- **Использование:** `tsc` для сборки проекта

#### **ts-node** (^10.9.2)
- **Что это:** Запуск TypeScript файлов напрямую
- **Зачем:** Запуск .ts файлов без компиляции
- **Использование:** `ts-node src/server.ts` для разработки

#### **nodemon** (^3.0.2)
- **Что это:** Автоматическая перезагрузка сервера при изменениях
- **Зачем:** Удобство разработки — не нужно перезапускать вручную
- **Использование:** `nodemon --exec ts-node src/server.ts`

#### **concurrently** (^8.2.2)
- **Что это:** Запуск нескольких команд одновременно
- **Зачем:** Одновременный запуск сервера и клиента
- **Использование:** `concurrently "npm run dev:server" "npm run dev:client"`

#### **@typescript-eslint/*** пакеты
- **Что это:** ESLint плагины для TypeScript
- **Зачем:** Проверка качества кода TypeScript
- **Пакеты:**
  - `@typescript-eslint/eslint-plugin` — правила для TS
  - `@typescript-eslint/parser` — парсер для TS

#### **eslint** (^8.56.0)
- **Что это:** Линтер для JavaScript/TypeScript
- **Зачем:** Проверка кода на ошибки и стиль
- **Использование:** Автоматическая проверка кода

## 🚀 Как выполнить установку?

### Базовый способ

```bash
npm install
```

или сокращенно:

```bash
npm i
```

### Что происходит пошагово:

1. **npm читает package.json**
   ```
   📄 package.json → Анализ зависимостей
   ```

2. **Создается/очищается node_modules/**
   ```
   📁 node_modules/ → Создание папки
   ```

3. **Скачивание пакетов**
   ```
   🌐 npm registry → Скачивание express, typeorm, и т.д.
   ```

4. **Установка зависимостей**
   ```
   📦 Установка в node_modules/
   ```

5. **Создание package-lock.json**
   ```
   🔒 package-lock.json → Фиксация версий
   ```

### Время выполнения

- **Первый раз:** 2-5 минут (зависит от скорости интернета)
- **Повторно:** 10-30 секунд (если node_modules уже есть)

## 📊 Что создается после установки?

### Структура после npm install:

```
marina-crm/
├── node_modules/          ← Все установленные пакеты (может быть 100+ МБ)
│   ├── express/
│   ├── typeorm/
│   ├── pg/
│   └── ... (сотни папок)
├── package-lock.json       ← Файл с точными версиями
└── package.json            ← Ваш файл (не изменяется)
```

### package-lock.json

Этот файл:
- ✅ Фиксирует точные версии всех пакетов
- ✅ Гарантирует одинаковые версии у всех разработчиков
- ✅ Ускоряет повторную установку
- ✅ **НЕ должен** быть в .gitignore (должен быть в git)

## ⚠️ Возможные проблемы и решения

### Проблема 1: Ошибка "npm is not recognized"

**Решение:**
```bash
# Установите Node.js с официального сайта
# https://nodejs.org/
```

### Проблема 2: Ошибка "EACCES: permission denied"

**Решение (Windows):**
- Запустите терминал от имени администратора
- Или используйте PowerShell/CMD с правами администратора

**Решение (Linux/Mac):**
```bash
# НЕ используйте sudo для npm install
# Вместо этого исправьте права на папку:
sudo chown -R $(whoami) ~/.npm
```

### Проблема 3: Медленная установка

**Решение:**
```bash
# Используйте npm с кешем
npm install --prefer-offline

# Или используйте yarn (быстрее)
npm install -g yarn
yarn install
```

### Проблема 4: Ошибки с зависимостями

**Решение:**
```bash
# Очистите кеш и переустановите
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### Проблема 5: Конфликты версий

**Решение:**
```bash
# Установите конкретную версию Node.js
# Используйте nvm (Node Version Manager)
nvm install 18
nvm use 18
```

## 📝 Полезные команды

### Установка конкретного пакета

```bash
# Production зависимость
npm install express

# Development зависимость
npm install --save-dev typescript

# Глобальная установка
npm install -g nodemon
```

### Обновление зависимостей

```bash
# Проверка устаревших пакетов
npm outdated

# Обновление всех пакетов
npm update

# Обновление конкретного пакета
npm install express@latest
```

### Просмотр установленных пакетов

```bash
# Список всех установленных пакетов
npm list

# Список только top-level пакетов
npm list --depth=0

# Поиск пакета
npm list express
```

### Удаление пакетов

```bash
# Удаление пакета
npm uninstall express

# Удаление всех пакетов
rm -rf node_modules package-lock.json
```

## ✅ Проверка успешной установки

После выполнения `npm install` проверьте:

1. **Папка node_modules создана:**
   ```bash
   ls node_modules
   # Должны быть папки: express, typeorm, pg и т.д.
   ```

2. **package-lock.json создан:**
   ```bash
   ls package-lock.json
   # Файл должен существовать
   ```

3. **Нет ошибок в консоли:**
   - Должно быть сообщение о количестве установленных пакетов
   - Не должно быть красных ошибок

## 🎓 Дополнительная информация

### Семантическое версионирование (^)

В `package.json` используется символ `^`:
- `"express": "^4.18.2"` означает:
  - Минимальная версия: `4.18.2`
  - Максимальная: `4.x.x` (но не `5.0.0`)
  - npm установит последнюю совместимую версию

### Размер node_modules

- Может быть **100-500 МБ** или больше
- Это нормально — каждый пакет имеет свои зависимости
- **НЕ добавляйте** `node_modules/` в git (уже в .gitignore)

### Альтернативы npm

- **yarn** — быстрее, лучше кеширование
- **pnpm** — экономит место на диске
- **npm** — стандартный менеджер (используем в проекте)

## 🚀 Следующий шаг

После успешной установки:

```bash
# 1. Настройте базу данных
# 2. Создайте .env файл
# 3. Запустите миграции
npm run migrate

# 4. Заполните тестовыми данными
npm run seed

# 5. Запустите сервер
npm run dev:server
```

---

**Готово!** Теперь вы знаете все о `npm install` и можете успешно установить зависимости проекта Marina CRM.



