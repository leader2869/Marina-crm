# 🎨 Настройка Frontend приложения

## ✅ Что создано

Создана полная структура frontend приложения на React + TypeScript + Vite с Tailwind CSS.

### Структура проекта:

```
client/
├── src/
│   ├── components/          # Компоненты
│   │   ├── Layout.tsx       # Основной layout с сайдбаром
│   │   └── ProtectedRoute.tsx  # Защита маршрутов
│   ├── contexts/            # React контексты
│   │   └── AuthContext.tsx  # Контекст аутентификации
│   ├── pages/               # Страницы
│   │   ├── Login.tsx        # Страница входа
│   │   ├── Register.tsx     # Страница регистрации
│   │   ├── Dashboard.tsx    # Дашборд
│   │   ├── Clubs.tsx        # Список клубов
│   │   ├── ClubDetails.tsx  # Детали клуба
│   │   ├── Vessels.tsx      # Список судов
│   │   ├── Bookings.tsx     # Бронирования
│   │   ├── Finances.tsx     # Финансы
│   │   └── Payments.tsx     # Платежи
│   ├── services/            # API сервисы
│   │   └── api.ts           # Axios клиент и сервисы
│   ├── types/               # TypeScript типы
│   │   └── index.ts         # Все типы данных
│   ├── App.tsx              # Главный компонент
│   ├── main.tsx             # Точка входа
│   └── index.css            # Глобальные стили
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
└── tailwind.config.js
```

## 🚀 Установка и запуск

### ШАГ 1: Установка зависимостей

```bash
cd client
npm install
```

### ШАГ 2: Настройка переменных окружения

Создайте файл `client/.env`:

```env
VITE_API_URL=http://localhost:3001/api
```

### ШАГ 3: Запуск frontend

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### ШАГ 4: Запуск backend и frontend вместе

Из корня проекта:

```bash
npm run dev
```

Это запустит оба сервера одновременно.

## 🎨 Особенности интерфейса

### Дизайн:
- ✅ Современный и чистый интерфейс
- ✅ Адаптивный дизайн (мобильные устройства)
- ✅ Tailwind CSS для стилизации
- ✅ Lucide React для иконок
- ✅ Темная/светлая тема (базовая поддержка)

### Компоненты:
- ✅ Боковое меню (сайдбар) с навигацией
- ✅ Верхняя панель с информацией о пользователе
- ✅ Защищенные маршруты (требуют авторизации)
- ✅ Формы входа и регистрации
- ✅ Карточки для отображения данных
- ✅ Таблицы для списков

### Страницы:
1. **Login** - Страница входа
2. **Register** - Страница регистрации
3. **Dashboard** - Дашборд с общей статистикой
4. **Clubs** - Список яхт-клубов с поиском
5. **ClubDetails** - Детальная информация о клубе
6. **Vessels** - Список судов
7. **Bookings** - Таблица бронирований
8. **Finances** - Финансовый модуль (в разработке)
9. **Payments** - Таблица платежей

## 🔐 Аутентификация

- JWT токены хранятся в localStorage
- Автоматическое добавление токена к запросам
- Редирект на /login при истечении токена
- Защита маршрутов через ProtectedRoute

## 📱 Адаптивность

Интерфейс адаптирован для:
- 📱 Мобильных устройств (меню-гамбургер)
- 💻 Планшетов
- 🖥️ Десктопов

## 🎯 Следующие шаги

После установки зависимостей:

1. **Установите зависимости:**
   ```bash
   cd client
   npm install
   ```

2. **Создайте .env файл:**
   ```bash
   echo "VITE_API_URL=http://localhost:3001/api" > client/.env
   ```

3. **Запустите frontend:**
   ```bash
   cd client
   npm run dev
   ```

4. **Откройте браузер:**
   ```
   http://localhost:5173
   ```

## 🐛 Решение проблем

### Ошибка: "Cannot find module"

```bash
cd client
npm install
```

### Ошибка: "Port 5173 is already in use"

Измените порт в `vite.config.ts`:
```typescript
server: {
  port: 5174, // или другой свободный порт
}
```

### Ошибка подключения к API

Проверьте:
1. Backend сервер запущен на порту 3001
2. В `.env` указан правильный URL: `VITE_API_URL=http://localhost:3001/api`
3. CORS настроен правильно в backend

## 📚 Документация

- [React](https://react.dev/)
- [Vite](https://vitejs.dev/)
- [Tailwind CSS](https://tailwindcss.com/)
- [React Router](https://reactrouter.com/)

---

**Готово!** Frontend приложение создано и готово к использованию. 🎉

