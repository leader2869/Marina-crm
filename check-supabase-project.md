# 🔍 Проверка проекта Supabase

## ❌ Ошибка: `ENODATA db.toimcbqcpzusbrbqwxqn.supabase.co`

DNS не может найти этот хост. Это означает, что проект либо не существует, либо приостановлен.

---

## ✅ Пошаговая проверка

### ШАГ 1: Проверьте статус проекта в Supabase Dashboard

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Войдите в свой аккаунт
3. Найдите проект с PROJECT-REF: `toimcbqcpzusbrbqwxqn`

**Проверьте статус проекта:**

- ✅ **Active** (зеленый) - проект активен
- ⏸️ **Paused** (желтый) - проект приостановлен
- ❌ **Deleted** (красный) - проект удален
- ⏳ **Initializing** - проект еще создается

### ШАГ 2: Если проект приостановлен

Если проект приостановлен:

1. Нажмите на проект
2. Найдите кнопку **"Restore"** или **"Resume"**
3. Подождите 1-2 минуты, пока проект восстановится
4. После восстановления хост должен стать доступным

### ШАГ 3: Если проект удален

Если проект удален:

1. Создайте новый проект в Supabase
2. Получите новый connection string
3. Обновите `.env` файл с новыми данными

### ШАГ 4: Проверьте правильность PROJECT-REF

1. В Supabase Dashboard откройте проект
2. Перейдите в **Settings** → **General**
3. Найдите **Reference ID** - это ваш PROJECT-REF
4. Убедитесь, что он совпадает с хостом в `.env`

**Формат хоста должен быть:**
```
db.[PROJECT-REF].supabase.co
```

Где `[PROJECT-REF]` - это Reference ID из Settings → General

### ШАГ 5: Попробуйте получить connection string заново

1. В Supabase Dashboard откройте проект
2. Перейдите в **Settings** → **Database**
3. Прокрутите до раздела **"Connection string"**
4. Выберите вкладку **"URI"**
5. Скопируйте connection string заново
6. Обновите `.env` файл

---

## 🔄 Альтернативные решения

### Вариант 1: Использование Connection Pooler

Если direct connection не работает, попробуйте connection pooler:

1. В Supabase Dashboard → Settings → Database
2. Найдите раздел **"Connection pooling"**
3. Скопируйте connection string для pooler
4. Обновите `.env`:

```env
DATABASE_URL=postgresql://postgres.toimcbqcpzusbrbqwxqn:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

### Вариант 2: Проверка через Supabase CLI

Если у вас установлен Supabase CLI:

```bash
supabase projects list
```

Это покажет все ваши проекты и их статусы.

### Вариант 3: Создание нового проекта

Если проект действительно не существует:

1. Создайте новый проект в Supabase
2. Используйте новое имя (например, `marina-crm-new`)
3. Получите новый connection string
4. Обновите `.env` файл

---

## 📝 Проверка через браузер

Попробуйте открыть хост в браузере (для проверки):

```
https://db.toimcbqcpzusbrbqwxqn.supabase.co
```

Если хост существует, вы увидите какую-то информацию или ошибку подключения.
Если хост не существует, браузер покажет ошибку "Сайт не найден".

---

## ✅ Чек-лист

- [ ] Открыл Supabase Dashboard
- [ ] Проверил статус проекта (Active/Paused/Deleted)
- [ ] Если Paused - нажал Restore/Resume
- [ ] Проверил Reference ID в Settings → General
- [ ] Сравнил Reference ID с хостом в .env
- [ ] Скопировал connection string заново из Settings → Database
- [ ] Обновил .env файл
- [ ] Запустил диагностику снова: `npm run diagnose-supabase`

---

## 🆘 Если ничего не помогает

1. **Создайте новый проект** в Supabase
2. **Используйте новый connection string**
3. **Обновите .env файл**
4. **Запустите миграцию заново**

---

**После проверки статуса проекта и обновления .env файла, запустите диагностику снова!**

