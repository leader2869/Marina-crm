# Быстрый старт

## 📋 Предварительные требования

- Node.js 18+ 
- PostgreSQL 14+
- npm или yarn

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE marina_crm;
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые параметры:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=marina_crm
DB_USER=postgres
DB_PASSWORD=your_password
JWT_SECRET=your-super-secret-jwt-key
```

### 4. Инициализация базы данных

```bash
npm run migrate
```

### 5. Заполнение тестовыми данными (опционально)

```bash
npm run seed
```

Это создаст:
- Суперадминистратора: `admin@marina-crm.com` / `admin123`
- Владельца клуба: `owner@yachtclub.com` / `owner123`
- Судовладельца: `vessel@owner.com` / `vessel123`
- Тестовый яхт-клуб с 50 причалами
- Категории расходов

### 6. Запуск сервера

**Режим разработки:**
```bash
npm run dev:server
```

**Продакшн:**
```bash
npm run build
npm start
```

Сервер будет доступен по адресу: `http://localhost:3000`

## 📝 Проверка работы

### Health check

```bash
curl http://localhost:3000/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "timestamp": "2024-05-15T10:00:00.000Z"
}
```

### Тест регистрации

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "test123",
    "firstName": "Тест",
    "lastName": "Тестов"
  }'
```

### Тест входа

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@marina-crm.com",
    "password": "admin123"
  }'
```

## 📚 Документация

- [Архитектура системы](ARCHITECTURE.md)
- [Примеры использования API](API_EXAMPLES.md)
- [README](README.md)

## 🔧 Структура проекта

```
marina-crm/
├── src/
│   ├── server.ts              # Точка входа
│   ├── config/               # Конфигурация
│   ├── database/             # Миграции и seed
│   ├── entities/             # Модели данных
│   ├── modules/              # Бизнес-модули
│   │   ├── auth/            # Аутентификация
│   │   ├── clubs/           # Яхт-клубы
│   │   ├── vessels/         # Судна
│   │   ├── bookings/        # Бронирования
│   │   ├── finances/        # Финансы
│   │   └── payments/        # Платежи
│   ├── middleware/          # Middleware
│   ├── utils/               # Утилиты
│   └── types/               # TypeScript типы
├── package.json
├── tsconfig.json
└── README.md
```

## 🐛 Решение проблем

### Ошибка подключения к БД

Проверьте:
- PostgreSQL запущен
- Параметры подключения в `.env` корректны
- База данных создана

### Ошибка порта занят

Измените `PORT` в `.env` или остановите процесс, использующий порт 3000

### Ошибки TypeScript

Убедитесь, что установлены все зависимости:
```bash
npm install
```

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи сервера
2. Настройки базы данных
3. Переменные окружения

