# 🔍 Отладка ошибки входа

## 📋 Шаг 1: Проверить консоль браузера

1. Откройте frontend в браузере
2. Нажмите **F12** (или **Ctrl+Shift+I**)
3. Перейдите на вкладку **Console**
4. Попробуйте войти
5. Найдите сообщения об ошибках

**Что искать:**
- `API Error:` - детали ошибки API
- `Login error:` - ошибка при входе
- `status:` - код статуса HTTP (401, 404, 500, и т.д.)
- `data:` - данные об ошибке от сервера

## 🔧 Шаг 2: Проверить настройки API URL

### Проверка файла `.env`

Убедитесь, что файл `client/.env` существует и содержит:

```env
VITE_API_URL=https://marina-crm.vercel.app/api
```

**Важно:** Замените `marina-crm.vercel.app` на ваш реальный URL из Vercel Dashboard.

### Проверка в консоли браузера

В консоли браузера выполните:
```javascript
console.log('API URL:', import.meta.env.VITE_API_URL)
```

Должен показать правильный URL API.

## 🗄️ Шаг 3: Проверить наличие пользователя в базе данных

### Вариант 1: Через Supabase Dashboard

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Table Editor**
4. Найдите таблицу `user`
5. Проверьте, есть ли пользователь с нужным email/телефоном

### Вариант 2: Создать тестового пользователя

Если пользователя нет, создайте его через API регистрации или напрямую в базе данных.

## 🔍 Шаг 4: Проверить логи Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Найдите ваш проект `Marina-crm`
3. Перейдите в **Deployments**
4. Выберите последний деплой
5. Откройте **Logs**
6. Попробуйте войти через frontend
7. Найдите логи с `[Auth Router]` и `[DB Init]`

**Что искать:**
- `✅ База данных подключена` - БД работает
- `[Auth Router] POST /login` - запрос доходит до роутера
- Ошибки подключения к БД
- Ошибки обработки запроса

## 🐛 Типичные ошибки и решения

### Ошибка: "Ошибка сети" или "Не удалось подключиться к серверу"

**Причины:**
1. Неправильный `VITE_API_URL`
2. CORS ошибка
3. Сервер недоступен

**Решение:**
1. Проверьте `VITE_API_URL` в `client/.env`
2. Убедитесь, что сервер работает (проверьте `/health`)
3. Проверьте CORS настройки в Vercel

### Ошибка: "База данных не подключена"

**Причины:**
1. Переменные окружения не настроены в Vercel
2. Неправильный connection string
3. Проект Supabase приостановлен

**Решение:**
1. Проверьте переменные окружения в Vercel Dashboard
2. Убедитесь, что `DATABASE_URL` установлен правильно
3. Проверьте статус проекта Supabase

### Ошибка: "Неверный email/телефон или пароль"

**Причины:**
1. Пользователь не существует
2. Неправильный пароль
3. Пользователь неактивен (`isActive = false`)

**Решение:**
1. Проверьте, существует ли пользователь в базе данных
2. Проверьте правильность пароля
3. Убедитесь, что `isActive = true` для пользователя

### Ошибка: 404 "Маршрут не найден"

**Причины:**
1. Неправильный URL API
2. Маршрут не зарегистрирован

**Решение:**
1. Проверьте `VITE_API_URL` - должен заканчиваться на `/api`
2. Проверьте логи Vercel - должен быть `[Auth Router] POST /login`

### Ошибка: 500 "Ошибка сервера"

**Причины:**
1. Ошибка в коде сервера
2. Проблема с базой данных
3. Неправильная обработка запроса

**Решение:**
1. Проверьте логи Vercel для деталей ошибки
2. Проверьте подключение к базе данных
3. Проверьте, что все переменные окружения установлены

## 📝 Шаг 5: Проверить запрос в Network tab

1. Откройте **DevTools** (F12)
2. Перейдите на вкладку **Network**
3. Попробуйте войти
4. Найдите запрос к `/api/auth/login`
5. Проверьте:
   - **Method:** должен быть `POST`
   - **Status:** код статуса (200, 401, 404, 500, и т.д.)
   - **Request Payload:** данные запроса
   - **Response:** ответ от сервера

## ✅ Шаг 6: Тестирование через curl

Попробуйте сделать запрос напрямую через curl:

```bash
curl -X POST https://marina-crm.vercel.app/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"emailOrPhone":"admin@marina-crm.com","password":"admin123"}'
```

Замените `marina-crm.vercel.app` на ваш реальный URL.

**Ожидаемый ответ:**
```json
{
  "message": "Вход выполнен успешно",
  "token": "...",
  "user": {
    "id": 1,
    "email": "admin@marina-crm.com",
    "firstName": "Admin",
    "lastName": "User",
    "role": "super_admin",
    "isValidated": true
  }
}
```

## 🔐 Создание тестового пользователя

Если пользователя нет, создайте его:

### Через API регистрации:

```bash
curl -X POST https://marina-crm.vercel.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "test123",
    "firstName": "Test",
    "lastName": "User",
    "phone": "+79991234567",
    "role": "vessel_owner"
  }'
```

### Через Supabase Dashboard:

1. Откройте **Table Editor**
2. Найдите таблицу `user`
3. Нажмите **Insert row**
4. Заполните поля:
   - `email`: `test@example.com`
   - `password`: (хэшированный пароль)
   - `firstName`: `Test`
   - `lastName`: `User`
   - `phone`: `+79991234567`
   - `role`: `vessel_owner`
   - `isActive`: `true`
   - `isValidated`: `true`

**Важно:** Пароль должен быть хэширован с помощью bcrypt.

## 📞 Если проблема не решена

1. Проверьте все логи (браузер, Vercel)
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что база данных подключена
4. Проверьте, что пользователь существует и активен
5. Пришлите детали ошибки из консоли браузера и логов Vercel

