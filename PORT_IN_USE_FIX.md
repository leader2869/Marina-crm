# 🔧 Решение проблемы: Порт 3000 занят

## ❌ Ошибка: `EADDRINUSE: address already in use :::3000`

Эта ошибка означает, что порт 3000 уже используется другим процессом.

## ✅ Решение

### Способ 1: Остановить процесс, использующий порт (рекомендуется)

1. **Найдите процесс, использующий порт 3000:**
   ```bash
   netstat -ano | findstr :3000
   ```

2. **Остановите процесс по PID:**
   ```bash
   taskkill /F /PID <PID>
   ```
   (Замените `<PID>` на номер процесса из предыдущей команды)

3. **Проверьте, что порт свободен:**
   ```bash
   netstat -ano | findstr :3000
   ```
   (Не должно быть вывода)

4. **Запустите сервер снова:**
   ```bash
   npm run dev:server
   ```

### Способ 2: Изменить порт в настройках

1. **Откройте файл `.env`**

2. **Измените порт:**
   ```env
   PORT=3001
   ```
   (или любой другой свободный порт)

3. **Запустите сервер:**
   ```bash
   npm run dev:server
   ```

### Способ 3: Автоматическое освобождение порта (скрипт)

Создайте файл `kill-port.ps1`:

```powershell
# kill-port.ps1
$port = 3000
$processes = Get-NetTCPConnection -LocalPort $port -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique

if ($processes) {
    foreach ($pid in $processes) {
        Write-Host "Останавливаю процесс $pid на порту $port"
        Stop-Process -Id $pid -Force
    }
    Write-Host "✅ Порт $port освобожден"
} else {
    Write-Host "✅ Порт $port уже свободен"
}
```

Запустите:
```bash
powershell -ExecutionPolicy Bypass -File kill-port.ps1
```

## 🔍 Проверка занятых портов

### Windows (PowerShell):
```powershell
Get-NetTCPConnection -LocalPort 3000 | Select-Object LocalAddress, LocalPort, State, OwningProcess
```

### Windows (CMD):
```bash
netstat -ano | findstr :3000
```

### Linux/Mac:
```bash
lsof -i :3000
```

## 💡 Полезные команды

### Найти все процессы Node.js:
```bash
tasklist | findstr node
```

### Остановить все процессы Node.js:
```bash
taskkill /F /IM node.exe
```

⚠️ **Внимание:** Это остановит ВСЕ процессы Node.js, включая другие приложения!

## 📝 Предотвращение проблемы

### Использовать другой порт для разработки:

В `.env`:
```env
PORT=3001
```

### Проверять порт перед запуском:

Добавьте в `package.json`:
```json
{
  "scripts": {
    "dev:server": "node -e \"require('net').createServer().listen(3000, () => { process.exit(0) }).on('error', () => { process.exit(1) })\" && nodemon --exec ts-node src/server.ts"
  }
}
```

## ✅ После исправления

После освобождения порта запустите сервер:

```bash
npm run dev:server
```

Должно появиться:
```
✅ База данных подключена
🚀 Сервер запущен на порту 3000
📝 API доступен по адресу http://localhost:3000/api
```

---

**Готово!** Порт освобожден, сервер должен запуститься успешно.

