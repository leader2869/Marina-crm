# 🔧 Настройка базы данных для Vercel

## 📋 Шаг 1: Получить Connection String от Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings → Database**
4. Найдите раздел **Connection string**
5. Выберите **Session Pooler** (для IPv4 сетей) или **Direct connection** (для IPv6)
6. Скопируйте connection string

### Формат Connection String:

**Для Session Pooler (рекомендуется для Vercel):**
```
postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

**Пример:**
```
postgresql://postgres.toimcbqcpzusbrbqwxqn:MyPassword123@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```

**Для Direct Connection:**
```
postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
```

## 🔧 Шаг 2: Настроить переменные окружения в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Найдите ваш проект `Marina-crm`
3. Перейдите в **Settings → Environment Variables**
4. Добавьте следующие переменные:

### Обязательные переменные:

#### Вариант 1: Использование DATABASE_URL (рекомендуется)

```env
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

**Пример:**
```env
DATABASE_URL=postgresql://postgres.toimcbqcpzusbrbqwxqn:MyPassword123@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```

#### Вариант 2: Использование отдельных переменных

```env
DB_HOST=aws-0-[REGION].pooler.supabase.com
DB_PORT=6543
DB_NAME=postgres
DB_USER=postgres.[PROJECT-REF]
DB_PASSWORD=your_password
```

**Пример:**
```env
DB_HOST=aws-0-eu-central-1.pooler.supabase.com
DB_PORT=6543
DB_NAME=postgres
DB_USER=postgres.toimcbqcpzusbrbqwxqn
DB_PASSWORD=MyPassword123
```

### Дополнительные переменные:

```env
# JWT
JWT_SECRET=your_very_secure_secret_key_here
JWT_EXPIRES_IN=7d

# Frontend URL
FRONTEND_URL=https://marina-crm.vercel.app

# Node Environment
NODE_ENV=production
```

## ✅ Шаг 3: Передеплой проекта

После добавления переменных окружения:

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **⋮** (три точки)
4. Выберите **Redeploy**
5. Дождитесь завершения сборки

## 🔍 Шаг 4: Проверить подключение

### 1. Проверка health endpoint:

Откройте в браузере:
```
https://marina-crm.vercel.app/health
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "timestamp": "2025-01-10T19:00:00.000Z"
}
```

### 2. Проверка логов:

1. Откройте Vercel Dashboard → ваш проект
2. Перейдите в **Deployments**
3. Выберите последний деплой
4. Откройте **Logs**
5. Найдите строку: `✅ База данных подключена`

Если видите ошибку:
- `❌ Ошибка при подключении к базе данных` — проверьте переменные окружения
- `[DB Init] Ошибка инициализации БД` — проверьте connection string

## 🐛 Устранение проблем

### Ошибка: "База данных не подключена"

**Причины:**
1. Переменные окружения не установлены
2. Неправильный connection string
3. Неправильный пароль
4. Проект Supabase приостановлен

**Решение:**
1. Проверьте, что все переменные окружения установлены в Vercel Dashboard
2. Убедитесь, что используете правильный connection string (Session Pooler для IPv4)
3. Проверьте пароль в Supabase Dashboard
4. Убедитесь, что проект Supabase активен

### Ошибка: "connection refused"

**Причины:**
1. Неправильный хост или порт
2. Проект Supabase приостановлен
3. Firewall блокирует подключение

**Решение:**
1. Проверьте правильность хоста и порта в connection string
2. Убедитесь, что проект Supabase активен
3. Используйте Session Pooler (порт 6543) для Vercel

### Ошибка: "password authentication failed"

**Причины:**
1. Неправильный пароль
2. Неправильный формат connection string

**Решение:**
1. Проверьте пароль в Supabase Dashboard
2. Убедитесь, что connection string правильно отформатирован
3. Попробуйте сбросить пароль в Supabase Dashboard

## 📝 Важные замечания

1. **Используйте Session Pooler для Vercel** — это обеспечивает лучшую производительность и совместимость с IPv4
2. **Не коммитьте переменные окружения** — они должны быть только в Vercel Dashboard
3. **После изменения переменных окружения** нужно передеплоить проект
4. **Проверяйте логи** после каждого деплоя, чтобы убедиться, что БД подключена

## 🔐 Безопасность

- Используйте сильные пароли для базы данных
- Не делитесь connection string публично
- Регулярно обновляйте пароли
- Используйте разные пароли для development и production

