# 📦 Перенос данных без pg_dump

## ❌ Проблема: `pg_dump: command not found`

Если у вас не установлен `pg_dump` или он не доступен в PATH, используйте альтернативный способ переноса данных через TypeORM.

---

## ✅ Решение: Экспорт и импорт через TypeORM

### ШАГ 1: Экспорт данных из локальной БД

1. **Убедитесь, что `.env` файл настроен на локальную БД:**
   ```env
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=marina_crm
   DB_USER=postgres
   DB_PASSWORD=ваш_пароль_от_локальной_БД
   ```

2. **Запустите экспорт данных:**
   ```bash
   npm run export-data
   ```

3. **Результат:**
   - Данные будут экспортированы в файл `./exports/data_export_YYYY-MM-DDTHH-MM-SS.json`
   - Файл будет содержать все данные из базы данных в JSON формате

---

### ШАГ 2: Обновление .env для Supabase

1. **Обновите `.env` файл для подключения к Supabase:**
   ```env
   # Supabase Connection (Session Pooler для IPv4)
   DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
   
   # Или отдельные параметры:
   # DB_HOST=aws-0-[REGION].pooler.supabase.com
   # DB_PORT=6543
   # DB_NAME=postgres
   # DB_USER=postgres.[PROJECT-REF]
   # DB_PASSWORD=[PASSWORD]
   ```

---

### ШАГ 3: Импорт данных в Supabase

1. **Найдите файл экспорта:**
   ```bash
   # Файл будет в папке ./exports/
   # Например: ./exports/data_export_2025-11-10T14-30-00.json
   ```

2. **Запустите импорт данных:**
   ```bash
   npm run import-data ./exports/data_export_2025-11-10T14-30-00.json
   ```

   Замените `data_export_2025-11-10T14-30-00.json` на имя вашего файла экспорта.

3. **Результат:**
   - Все данные будут импортированы в Supabase
   - Связи между таблицами будут восстановлены
   - ID будут пересозданы автоматически

---

## ⚠️ Важные замечания

### Пароли пользователей

- Пароли пользователей **НЕ экспортируются** из соображений безопасности
- Пользователи с паролями `[REDACTED]` получат временный пароль `temp123`
- **ВАЖНО**: После импорта измените пароли для этих пользователей!

### Порядок импорта

Скрипт импортирует данные в правильном порядке с учетом зависимостей:
1. Категории расходов
2. Пользователи
3. Яхт-клубы
4. Места
5. Судна
6. Тарифы
7. Связи тарифов и мест
8. Правила бронирования
9. Бронирования
10. Платежи
11. Доходы и расходы
12. Бюджеты
13. Связи пользователей и клубов

### Ограничения

- **Платежи** могут быть импортированы не полностью, так как требуют точных ID бронирований
- **ID записей** будут пересозданы (новые ID)
- **Связи** будут восстановлены автоматически

---

## 📝 Пример полного процесса

### 1. Экспорт из локальной БД

```bash
# Убедитесь, что .env настроен на локальную БД
DB_HOST=localhost
DB_PORT=5432
DB_NAME=marina_crm
DB_USER=postgres
DB_PASSWORD=ваш_пароль

# Экспортируйте данные
npm run export-data
```

### 2. Обновите .env для Supabase

```env
# Supabase
DATABASE_URL=postgresql://postgres.toimcbqcpzusbrbqwxqn:MyPassword123@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```

### 3. Импорт в Supabase

```bash
# Импортируйте данные
npm run import-data ./exports/data_export_2025-11-10T14-30-00.json
```

---

## 🔍 Проверка после импорта

После импорта проверьте данные:

```bash
# Проверка пользователей
npm run check-admin

# Проверка клубов
npm run check-clubs

# Проверка валидации
npm run check-isvalidated
```

---

## 🆘 Если что-то пошло не так

### Проблема: "Пользователь уже существует"

Если пользователь с таким email уже существует:
- Скрипт пропустит его и использует существующего
- Связи будут восстановлены с существующим пользователем

### Проблема: "Клуб не найден"

Если клуб не может быть импортирован:
- Проверьте, что владелец клуба был импортирован
- Убедитесь, что пользователи импортированы первыми

### Проблема: "Пароли не работают"

Если не можете войти:
- Пользователи с `[REDACTED]` паролями имеют временный пароль `temp123`
- Измените пароли через интерфейс приложения
- Или используйте `npm run reset-admin` для сброса пароля суперадминистратора

---

## ✅ Альтернатива: Использование seed данных

Если у вас нет важных данных в локальной БД, можно просто использовать seed данные:

```bash
# Загрузите тестовые данные
npm run seed
```

Это создаст:
- Суперадминистратор: `admin@marina-crm.com` / `admin123`
- Владелец клуба: `owner@yachtclub.com` / `owner123`
- Судовладелец: `vessel@owner.com` / `vessel123`
- Тестовый яхт-клуб с 50 местами

---

## 📚 Дополнительная информация

- Экспортированные данные сохраняются в папке `./exports/`
- Формат: JSON (читаемый и редактируемый)
- Размер файла зависит от количества данных
- Можно экспортировать и импортировать несколько раз

---

**После импорта данных, ваш проект будет полностью готов к работе с Supabase! 🚀**

