# 🔍 Диагностика проблемы с сайтом

## Возможные причины, почему сайт не грузится:

### 1. Проблема с билдом на Vercel
- Проверьте логи деплоя в Vercel Dashboard → Deployments → выберите последний деплой → View Function Logs
- Убедитесь, что билд завершился успешно

### 2. Проблема с переменными окружения
Проверьте в Vercel Dashboard → Settings → Environment Variables наличие:
- `DATABASE_URL` или `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`
- `JWT_SECRET`
- `JWT_EXPIRES_IN`
- `FRONTEND_URL`
- `NODE_ENV=production`
- `VERCEL=1` (устанавливается автоматически)

### 3. Проблема с базой данных
- Проверьте, что база данных доступна
- Проверьте, что `DATABASE_URL` правильный (для Supabase используйте Session Pooler с портом 6543)
- Проверьте логи в Vercel Dashboard на наличие ошибок подключения к БД

### 4. Проблема с конфигурацией vercel.json
- Убедитесь, что `vercel.json` правильно настроен
- Проверьте, что `api/index.ts` существует и правильно экспортирует handler

### 5. Проблема с DNS
- Проверьте, что домен правильно настроен в Vercel
- Проверьте DNS записи через `nslookup www.1marina.ru` или `dig www.1marina.ru`

## Шаги для диагностики:

1. **Проверьте health endpoint:**
   ```bash
   curl https://www.1marina.ru/health
   ```
   Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. **Проверьте главную страницу:**
   ```bash
   curl -I https://www.1marina.ru
   ```
   Должен вернуть статус 200

3. **Проверьте логи Vercel:**
   - Откройте Vercel Dashboard
   - Перейдите в ваш проект
   - Откройте вкладку "Deployments"
   - Выберите последний деплой
   - Просмотрите "Function Logs" и "Build Logs"

4. **Проверьте переменные окружения:**
   - Vercel Dashboard → Settings → Environment Variables
   - Убедитесь, что все переменные установлены для Production

5. **Попробуйте передеплоить:**
   - В Vercel Dashboard → Deployments → выберите последний деплой → "Redeploy"

## Быстрое исправление:

1. Убедитесь, что все переменные окружения установлены
2. Проверьте логи деплоя на наличие ошибок
3. Попробуйте передеплоить проект
4. Если проблема с БД - проверьте `DATABASE_URL` и доступность базы данных




