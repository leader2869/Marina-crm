# 🚀 Запуск Frontend приложения

## ✅ Структура создана

Frontend приложение создано в папке `client/` с использованием:
- React 18 + TypeScript
- Vite (быстрый dev сервер)
- Tailwind CSS (стилизация)
- React Router (роутинг)
- Axios (HTTP клиент)

## 📦 ШАГ 1: Установка зависимостей

```bash
cd client
npm install
```

Это установит все необходимые пакеты для frontend.

## ⚙️ ШАГ 2: Настройка переменных окружения

Создайте файл `client/.env`:

```env
VITE_API_URL=http://localhost:3001/api
```

**Важно:** Убедитесь, что backend сервер работает на порту 3001!

## 🚀 ШАГ 3: Запуск frontend

```bash
cd client
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:5173**

## 🎯 ШАГ 4: Запуск backend и frontend вместе

Из корня проекта (если настроен скрипт `npm run dev`):

```bash
npm run dev
```

Это запустит:
- Backend на `http://localhost:3001`
- Frontend на `http://localhost:5173`

## 📱 Что доступно в интерфейсе

### Страницы:
1. **Вход** (`/login`) - Страница авторизации
2. **Регистрация** (`/register`) - Создание нового аккаунта
3. **Дашборд** (`/dashboard`) - Главная страница со статистикой
4. **Яхт-клубы** (`/clubs`) - Список всех клубов с поиском
5. **Детали клуба** (`/clubs/:id`) - Подробная информация о клубе
6. **Судна** (`/vessels`) - Список судов пользователя
7. **Бронирования** (`/bookings`) - Таблица всех бронирований
8. **Финансы** (`/finances`) - Финансовый модуль (в разработке)
9. **Платежи** (`/payments`) - Таблица платежей

### Особенности:
- ✅ Адаптивный дизайн (мобильные устройства)
- ✅ Боковое меню (сайдбар) с навигацией
- ✅ Защищенные маршруты (требуют авторизации)
- ✅ Автоматическое добавление токена к запросам
- ✅ Красивый современный интерфейс

## 🔐 Тестовые учетные данные

- **Суперадминистратор:** `admin@marina-crm.com` / `admin123`
- **Владелец клуба:** `owner@yachtclub.com` / `owner123`
- **Судовладелец:** `vessel@owner.com` / `vessel123`

## 🐛 Решение проблем

### Ошибка: "Cannot find module"

```bash
cd client
npm install
```

### Ошибка: "Port 5173 is already in use"

Измените порт в `client/vite.config.ts`:
```typescript
server: {
  port: 5174, // другой порт
}
```

### Ошибка подключения к API

Проверьте:
1. Backend сервер запущен на порту 3001
2. В `client/.env` указан правильный URL
3. CORS настроен в backend (уже настроен)

### Ошибка: "Failed to fetch"

Убедитесь, что:
- Backend сервер запущен
- URL в `.env` правильный
- Нет проблем с CORS

## 📝 Следующие шаги

После успешного запуска:

1. Откройте `http://localhost:5173` в браузере
2. Войдите с тестовыми учетными данными
3. Изучите интерфейс и функциональность
4. Начните разработку дополнительных функций

---

**Готово!** Frontend приложение готово к использованию! 🎉

