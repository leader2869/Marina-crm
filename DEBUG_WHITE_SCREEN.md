# 🐛 Отладка белого экрана - Инструкция

## 🔍 Что нужно сделать:

### Шаг 1: Откройте консоль браузера

1. Откройте `https://www.1marina.ru` в браузере
2. Нажмите `F12` или `Cmd + Option + I` (Mac)
3. Перейдите на вкладку **Console**

### Шаг 2: Найдите ошибки

В консоли должны быть логи. Найдите:
- ❌ **Красные ошибки** (Errors)
- ⚠️ **Желтые предупреждения** (Warnings)
- ℹ️ **Синие информационные сообщения** (Info)

### Шаг 3: Скопируйте ошибки

**Скопируйте ВСЕ ошибки из консоли** и пришлите мне. Особенно важны:

1. **Ошибки с текстом:**
   - `Failed to fetch`
   - `Network Error`
   - `CORS`
   - `Cannot read property`
   - `TypeError`
   - `ReferenceError`

2. **Логи API:**
   - `API URL: ...`
   - `VITE_API_URL: ...`
   - `PROD: ...`

3. **Ошибки загрузки профиля:**
   - `Error loading profile: ...`
   - `Profile loaded: ...`

### Шаг 4: Проверьте вкладку Network

1. В DevTools перейдите на вкладку **Network**
2. Очистите список (кнопка с крестиком)
3. Обновите страницу (`F5`)
4. Найдите запросы к `/api/*`
5. Посмотрите:
   - Статус код (200, 404, 500, и т.д.)
   - Ответ сервера (Response)
   - Заголовки (Headers)

### Шаг 5: Проверьте вкладку Sources

1. В DevTools перейдите на вкладку **Sources**
2. Посмотрите, загружаются ли файлы:
   - `index-C6WwqDfJ.js`
   - `index-Bhyywh62.css`

---

## 📸 Что прислать:

1. **Скриншот консоли браузера** с ошибками
2. **Текст ошибок** из консоли (скопируйте)
3. **Скриншот вкладки Network** с запросами к API
4. **Статус коды** API запросов

---

## 🔧 Временное решение:

Пока мы ищем проблему, попробуйте:

1. **Очистить localStorage:**
   - В консоли браузера выполните:
     ```javascript
     localStorage.clear()
     ```
   - Обновите страницу

2. **Открыть в режиме инкогнито:**
   - `Cmd + Shift + N` (Chrome/Safari)
   - `Cmd + Shift + P` (Firefox)

3. **Попробовать другой браузер:**
   - Chrome
   - Safari
   - Firefox

---

## 💡 Наиболее вероятные причины:

### 1. Ошибка при загрузке профиля пользователя
Если в localStorage есть токен, но он невалидный или истек, приложение может зависнуть.

**Решение:** Очистите localStorage (см. выше)

### 2. Проблема с API подключением
Приложение не может подключиться к API.

**Решение:** Проверьте логи Vercel и переменные окружения

### 3. Ошибка JavaScript
Ошибка в коде приложения.

**Решение:** Проверьте консоль браузера на наличие ошибок

---

## 🆘 Срочная помощь:

Если сайт критически важен и нужно быстрое решение:

1. **Очистите localStorage:**
   ```javascript
   localStorage.clear()
   location.reload()
   ```

2. **Проверьте, работает ли API:**
   ```bash
   curl https://www.1marina.ru/api/auth/profile
   ```
   Должен вернуть: `{"error":"Требуется аутентификация"}`

3. **Проверьте логи Vercel:**
   - Vercel Dashboard → Deployments → последний деплой → Function Logs

---

**Главное - пришлите ошибки из консоли браузера!** Это поможет точно определить проблему.





